[package]
description = "An empty package that sets the `package.metadata` required for wdk-build to generate WDM bindings for Rust"
edition = "2024"
license = "MIT OR Apache-2.0"
name = "config-wdm"
publish = false
version = "0.1.0"

[package.metadata.wdk.driver-model]
driver-type = "WDM"

[lib]
