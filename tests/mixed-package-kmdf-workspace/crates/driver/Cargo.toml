[package]
edition = "2024"
name = "driver"
publish = false
version = "0.0.0"

# This driver package uses the metadata from the workspace, but still needs this tag to signal that its a driver crate that needs packaging
[package.metadata.wdk]

[lib]
crate-type = ["cdylib"]
# temporarily disable due to github runner issues: https://github.com/microsoft/windows-drivers-rs/issues/502
test = false

[build-dependencies]
wdk-build.workspace = true

[dependencies]
wdk.workspace = true
wdk-alloc.workspace = true
wdk-panic.workspace = true
wdk-sys.workspace = true
